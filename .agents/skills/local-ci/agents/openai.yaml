interface:
  display_name: "Local CI"
  short_description: "Run GitHub Actions locally on demand"
  default_prompt: "Use $local-ci to run the repository's GitHub Actions workflows locally."
policy:
  allow_implicit_invocation: false
