# UI Package Instructions

## shadcn Components

- Run shadcn commands from `packages/ui`.
- Add the shadcn component first with `vpx shadcn add COMPONENT`.
- After adding or adapting a component, run `vp check` and any relevant build checks.
