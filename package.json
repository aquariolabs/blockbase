{
  "name": "dia-zero",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo '🔥 `dev` script not implemented'",
    "test:unit": "echo '🔥 `test:unit` script not implemented'",
    "setup:playwright": "echo '🔥 `setup:playwright` script not implemented'",
    "ready": "vp fmt && vp lint && vp run -r test && vp run -r build",
    "prepare": "vp config"
  },
  "devDependencies": {
    "portless": "catalog:",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": "24.15.0"
  },
  "packageManager": "pnpm@10.33.2"
}
