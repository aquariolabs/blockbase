{
  "name": "blockbase",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "setup:playwright": "vp run website#playwright install --with-deps",
    "ready": "vp fmt && vp lint && vp run -r test && vp run -r build",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@oxlint/plugins": "catalog:",
    "oxlint": "catalog:",
    "portless": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.22.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=26.0.0"
  }
}
